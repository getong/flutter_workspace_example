class Destination {
  const Destination(
    this.id,
    this.name,
    this.subtitle,
    this.rating,
    this.price,
    this.category,
    this.description,
  );
  final String id, name, subtitle, rating, category, description;
  final int price;
}
